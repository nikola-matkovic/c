#include <stdio.h>

void main(){
    int a; 
    scanf("%d", &a);
    (a%2==0) && printf("Broj je paran");
/*
    if(a%2==0)
        printf("Broj je paran");
    else
        printf("broj je neparan");
*/
}